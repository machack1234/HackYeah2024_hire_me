export type NavItem = {
  id: number;
  label: string;
  href: string;
};

export type TestimonialItem = {
  id: number;
  content: string;
  author: string;
  avatar: string;
};

export type FeatureItem = {
  id: number;
  title: string;
  desc: string;
};
